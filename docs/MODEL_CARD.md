# Model card — fusion-v1.0.0

*Generated by `scripts/make_model_card.py` from the stored evaluation results.*

## What it is for

RiskFusion estimates how likely it is that an online exam session contains behaviour a **human reviewer** should look
at, and orders sessions for review. Its output is a calibrated risk, a review tier (no action / routine / human /
priority), plain-language reasons and time-bounded moments to check.

**It is not for:** deciding that anyone cheated, penalising anyone automatically, ranking people, or any use without
a trained human reviewer who looks at the evidence. Its outputs contain no verdict by design (SRS CON-5, ETH-6).

## Model

- Gradient-boosted trees (LightGBM) over 271 session features from 50 signal families across 11 channels
  (features-v1.0.0); class imbalance handled with `scale_pos_weight`.
- **Monotone constraints on every feature** (direction learned on the training split). With the missing-channel
  imputation this *guarantees* a missing channel can never raise a score (FR-30).
- Platt calibration fitted on the dedicated calibration split.
- Operating point and tiers chosen from reviewer capacity: human review ≥ 0.108
  (3% of clean calibration sessions), priority ≥ 0.367 (0.5%), routine ≥ 0.033 (10%).
- Selected params {'num_leaves': 31, 'min_child_samples': 15, 'learning_rate': 0.06}, 238 trees, seed 20260923.

## Data

- **Training data is simulated** (sim-v1.1-s20260923-n5000): 5,000 sessions, 6.4% with violations, 10 behaviour profiles,
  8 nuisance factors, persistent confounders (phone on desk, background talk, writing, notifications, identity
  drift) and 35% evasive violations. Split by session and stratified: train 2,998 / validation 651 /
  calibration 601 / sealed holdout 750.
- Detector error rates are the SRS DR-3 **planning estimates made more pessimistic, not yet measured**. FR-6 code
  replaces them with rates measured on the consented mock corpus once it holds ≥ 20 analysed sessions.
- No real exam data (CON-1). No demographic attribute is a model input (ETH-4).

## Performance

| | PR-AUC | Recall at op. point | FPR | ECE |
|---|---|---|---|---|
| Rule baseline (validation) | 0.351 | 26.2% | 2.0% | 0.227 |
| Temporal ROCKET model (validation) | 0.828 | 81.0% | 2.5% | 0.007 |
| **Fusion (validation)** | **0.883** | **88.1%** | **4.4%** | **0.016** |
| Fusion, sealed holdout access 1 | 0.835 | 83.7% | 5.3% | 0.016 |
| **Fusion, sealed holdout (final)** | **0.835** (95% CI 0.73–0.92) | **83.7%** | **5.3%** | **0.016** |

Relative PR-AUC gain over the rule baseline on validation: 151% (target ≥ 15%).
Recall on evasive (subtle) violations: 61.5% vs 100.0% on overt ones.
Time-bounded flags reach IoU ≥ 0.5 for 84.4% of true episodes (target 70%).

## Fairness (FR-27, FR-28)

FPR disparity = highest ÷ lowest slice FPR (add-one smoothed), 95% bootstrap CI. Target < 1.3.

Validation:

| Factor | Disparity (CI) |
|---|---|
| lighting | 2.59 (1.56–9.04) |
| webcam class | 1.29 (1.09–3.61) |
| room noise | 1.10 (1.09–4.26) |
| connection stability | 2.21 (1.09–4.30) |
| eyewear | 1.10 (1.01–2.60) |
| head covering | 1.64 (1.02–3.52) |

Final holdout:

| Factor | Disparity (CI) | FPR per level |
|---|---|---|
| lighting | 1.40 (1.09–3.46) | bright: 6.3% (n=256), dim: 4.2% (n=177), normal: 5.1% (n=317) |
| webcam class | 1.17 (1.11–2.73) | hd: 5.8% (n=313), low: 4.9% (n=156), sd: 4.9% (n=281) |
| room noise | 1.83 (1.19–4.35) | moderate: 3.7% (n=290), noisy: 6.5% (n=106), quiet: 6.2% (n=354) |
| connection stability | 1.58 (1.02–2.72) | stable: 4.7% (n=584), unstable: 7.2% (n=166) |
| eyewear | 1.15 (1.01–2.31) | False: 5.6% (n=475), True: 4.7% (n=275) |
| head covering | 2.05 (1.09–3.74) | False: 4.7% (n=659), True: 9.1% (n=91) |

**The 1.3 target is not met for every factor.** Slices are small (most under 300 sessions), so the intervals are wide
and the point estimates unstable. Mitigation tested (FR-29): slice-balanced reweighting of clean sessions (lighting x webcam). It changed validation
FPR from 3.6% to 2.1% but widened lighting disparity
(2.44 → 2.62), so it was not adopted.

## Robustness

- Missing channels (FR-30): the largest share of sessions whose score rose when any single channel was removed is
  0.0% on the final holdout — guaranteed by construction, not only observed.
- Detector false positives +50% (FR-31), 800 new sessions: PR-AUC 0.915 → 0.889,
  FPR 4.0% → 7.1%, recall 88.2% → 88.2%.
  Degraded detectors are caught by the drift monitor (PSI alerts) before FPR drifts unnoticed.

## Ablations (validation PR-AUC)

| Group | Variant | PR-AUC |
|---|---|---|
| reference | all features | 0.883 |
| channel removed | without presence | 0.867 |
| channel removed | without identity | 0.746 |
| channel removed | without liveness | 0.880 |
| channel removed | without attention | 0.852 |
| channel removed | without environment | 0.856 |
| channel removed | without pose | 0.855 |
| channel removed | without audio_voice | 0.885 |
| channel removed | without audio_event | 0.853 |
| channel removed | without screen | 0.756 |
| channel removed | without device | 0.884 |
| channel removed | without behavioral | 0.871 |
| normalisation | baseline normalisation off | 0.876 |
| window set | whole-session only | 0.829 |
| window set | whole-session + 10 s | 0.847 |
| window set | whole-session + 60 s | 0.865 |
| window set | whole-session + 300 s | 0.864 |
| model family | rule baseline | 0.351 |
| model family | logistic regression | 0.890 |

## Known limitations and risks

- Trained and evaluated on **simulated** data. Real-world performance is unknown until the mock-corpus validation
  (FR-3, FR-6) and, ultimately, a properly governed pilot.
- The heuristic speaker detector (MFCC clustering) is weak; there is no licensed liveness model, so liveness is
  always reported unavailable; gaze is a head-pose proxy.
- Clean sessions are sent to review at 5.3% on the holdout, above the 3% budget set on calibration data;
  budgets must be re-checked on real data before use.
- Poor lighting, low-quality webcams and unstable connections change error rates. Reviewers must be told this.
- InsightFace weights are licensed for non-commercial research only.

## Ethical considerations

See `docs/FINAL_REPORT.md`, section "Harms of automated proctoring". Every output must be reviewed by a person;
candidates must be able to contest outcomes; missing data never counts against a candidate.
